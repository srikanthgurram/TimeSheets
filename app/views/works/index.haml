- content_for :head do
  %title All the Works details
/ Action Bar
= render "actions"

%h4 Works details
.row
  .table-responsive  
    %table.table.table-striped.table-hover    
      %thead
        %tr
          %th Employee
          %th Project
          %th Date
          %th Work details
          %th Hours
      %tbody
        = render "index"

- content_for(:aside) do
  = render "sidebar"
